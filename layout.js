export const metadata = {
  title: "Iqra Khan | Portfolio",
  description: "Premium Portfolio",
};

export default function RootLayout({ children }) {
  return (
    <html lang='en'>
      <body>{children}</body>
    </html>
  );
}
